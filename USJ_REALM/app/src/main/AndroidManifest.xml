<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hawk.usj_realm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".realm.person.Add">
        </activity>
        <activity android:name=".realm.person.Listar">
        </activity>
        <activity android:name=".realm.person.Update">
      </activity>
        <activity android:name=".realm.person.Delete">
        </activity>
        <activity android:name=".realm.pet.Add">
        </activity>
        <activity android:name=".realm.pet.Listar">
        </activity>
        <activity android:name=".realm.pet.Update">
        </activity>
        <activity android:name=".realm.pet.Delete">
        </activity>
    </application>

</manifest>